import React from "react";
import { Nav } from "./Styled";
interface Props {}

const Navbar: React.FC = (props: Props) => {
  return (
    <>
      <Nav></Nav>
    </>
  );
};

export default Navbar;
